<!DOCTYPE html>
<html>
<head>
  <title>Nodepop</title>
</head>
<body>
  <h1>Productos</h1>
  <ul>
    <% products.forEach(product => { %>
      <li><%= product.name %> - <%= product.price %>€</li>
    <% }); %>
  </ul>
</body>
</html>
